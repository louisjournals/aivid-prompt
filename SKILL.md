---
name: my-aivid-prompt
description: Use when 已确认的视频脚本、广告方案、分镜或镜头清单需要转化为可直接执行的 AI 视频提示词，尤其涉及可灵、Veo、Sora、Seedance、Higgsfield、人物一致性、参考图映射、Talking Head、低抽卡率、低成本或多镜头连续性时。
version: 1.1.0
---

# AI 视频提示词生成

## 目的

把已经批准的视频概念、广告脚本或分镜，转换成可直接执行的 AI 视频生成包。

保持原有广告策略、卖点、口播和 CTA，不擅自重新设计广告。只有当某个镜头明显不适合 AI 稳定生成时，才允许简化执行方式，但不能改变该镜头原本承担的广告作用。

核心原则：

> 按照模型真正能稳定生成的方式设计镜头，不要按照真实片场可以完成的方式幻想镜头。

## 输出语言

- 默认全部使用中文输出。
- AI 视频提示词、镜头说明、负面限制、参考图用途、生成顺序和后期说明都必须写中文。
- 只有模型名称、产品名称、按钮原文、品牌名或用户指定的准确英文文案可以保留英文。
- 不要为了显得专业而把主体提示词改写成英文。
- 用户使用中文时，不得输出大段英文提示词。

## 使用范围

适用于：

- AI 生成广告视频
- Facebook、Instagram、TikTok Talking Head 广告
- 人物主导的短视频
- 产品、汽车、门店、服务类广告
- 文生视频、图生视频、首帧生成、首尾帧生成、元素参考工作流
- 需要锁定人物、服装、车辆、产品、门店或场景一致性的多镜头项目
- 需要控制抽卡次数、生成成本和返工风险的提示词包

不适用于：

- 在没有已确认概念时重新决定广告策略
- 从零分析受众、Offer、漏斗或 CTA
- 操作剪辑软件完成最终成片
- 只写“电影感、震撼、高清”之类没有执行细节的空泛提示词

当用户同时要求广告策略和 AI 提示词时，必须先完成或确认策略、口播和分镜，再进入本 Skill。

## 必要输入

先从对话、视频、截图和文件中提取，不能重复询问已经提供的内容：

- 目标模型及版本
- 发布平台与画幅
- 目标时长
- 已确认的脚本、广告结构或分镜
- 人物参考图
- 服装参考图
- 产品、车辆、场景、门店和品牌参考图
- 对白语言与音频处理方式
- CTA 与准确文案
- 预算或可接受的抽卡次数

只有缺失的信息会明显改变生成方式时，才一次性集中提问。画面里能确认的内容不得要求用户重新解释。

## 默认制作决策

当用户强调低成本和低抽卡率时，默认执行以下规则：

1. 把整支视频拆成多个短镜头，不生成一条超长视频。
2. 每镜只保留一个主要人物和一个主要动作。
3. 有参考图时优先图生视频或首帧生成，不优先纯文生视频。
4. 运镜默认固定、轻微推近或轻微横移，只使用一种运动。
5. 字幕、价格、电话号码、Logo、准确 UI、CTA 和小字默认后期添加。
6. 复杂口型会提高成本时，先生成无声画面，再统一配音或 Lip Sync。
7. 所有重复出现的人物、服装、车辆、产品和场景必须持续使用同一套已批准参考图。
8. 先生成两支锚点测试镜头，确认人物和整体风格后，才继续整批生成。
9. 不允许一开始同时生成全部镜头。
10. 生成失败后必须降低复杂度，不能原样重复同一提示词抽卡。

## 可灵 3.0 默认适配

用户指定可灵 3.0 时，默认：

- 竖版广告采用 9:16，除非 brief 另有要求。
- 单镜可使用 3–15 秒，但可控广告镜头优先 4–9 秒。
- Talking Head、产品特写、工位动作和道路测试优先单镜模式。
- 只有两到三个简单镜头确实需要连续时，才使用多镜头模式。
- 人物、产品或车辆可绑定元素时必须绑定。
- 关键人物镜头优先使用干净首帧加人物参考元素。
- 不要求模型凭空生成准确品牌招牌、Logo 或小字。
- 若独立配音和 Lip Sync 更便宜、更容易修改，不自动使用原生音频。
- 对白较长时拆成多个短口播镜头，再以 B-roll 遮盖剪接点。

## AI 可行性检查

写提示词前，先给每镜分级。

### 绿色：可直接生成

- 固定机位或缓慢推近的 Talking Head
- 单人、小幅度手势
- 静止车辆展示
- 车灯近景亮起
- 一名技师执行一个明确动作
- 产品静态展示
- 固定机位夜间道路视角
- 静止主体周围出现简单扫描线或光效

### 黄色：先简化再生成

- 人物边走边讲话
- 摄影机绕人物或车辆运动
- 两个人互动
- 手部操作很小的机械零件
- 大面积反光车身配合动态灯光
- 在同一镜完成 Before／After 变化
- 高度依赖复杂品牌环境的门店画面

### 红色：必须替换或拆镜

- 多个人同时做不同动作
- 高速驾驶、漂移、碰撞或复杂交通
- 一镜同时包含进店、对白、产品展示和转场
- 大量可读文字的复杂悬浮 UI
- 一镜完成完整维修或安装流程
- 强运镜、准确口型、手部操作同时出现
- 车辆在运动中准确完成复杂改装变化

黄色或红色镜头不能直接照原分镜写 Prompt。必须先给出更稳定的替代方案。

## 参考图映射

每个镜头都必须明确说明每张参考图负责锁定什么：

- **人物参考图：** 面部、年龄感、发型、肤色、身体比例
- **服装参考图：** 衣服、鞋、配饰、制服细节
- **车辆参考图：** 车型、车身比例、颜色、轮圈、车灯、饰条
- **产品参考图：** 形状、材质、包装、镜片、灯泡、Projector
- **场景参考图：** 建筑、门店布局、招牌位置、灯光、工作台
- **构图参考图：** 景别、机位高度、人物与物件位置
- **首帧参考图：** 需要准确控制起始构图时使用
- **尾帧参考图：** 只有必须落在特定动作或构图时使用

禁止只写“使用附上的参考图”。必须写明每张图的控制职责。

### 可直接提交的参考图语法

最终可直接提交给 Higgsfield、可灵或 Seedance 的 Prompt，必须按照实际上传顺序显式引用参考图：

- 第一张图写作 `@[Image 1](image_1)`
- 第二张图写作 `@[Image 2](image_2)`
- 后续图片依次编号，不得跳号或凭文件名猜顺序

例如：`使用@[Image 1](image_1)作为唯一主讲人，人物站在@[Image 2](image_2)中的专业汽车灯光展示厅内。`

每次调整上传顺序，都必须同步更新 Prompt 内所有 Image 编号。最终 Prompt 不得只使用【主讲人】、【主展厅】等内部简称；这些简称只能用于规划区，不能替代平台实际识别的参考图标记。

## 全局一致性锁定

在逐镜提示词前，先写一段可重复使用的一致性设定，包括：

- 主人物标准描述
- 固定服装描述
- 固定声音、语气和表演方式
- 品牌与场景标准描述
- 每个重复车辆或产品的标准描述
- 固定画幅、色温、灯光与画面质感
- 所有不得改变的连续性规则

逐镜 Prompt 不要反复复制全部长描述，使用统一命名，如：

- 【主讲人】
- 【主车辆】
- 【固定服装】
- 【主展厅】
- 【主工作区】

## Facebook Talking Head 规则

针对 Facebook 直接转化型口播广告：

- 默认胸口以上或腰部以上构图
- 第一帧人物已经看向镜头，不要从发呆开始
- 保持直接眼神接触
- 每镜只说一个意思
- 每句话最多一个小手势
- 除非走动本身就是 Hook，否则不要边走边讲
- 对白时不要同时拿产品、拆零件或做复杂动作
- 摄影机固定或非常缓慢推近
- 背景简单，但必须让人看得出业务场景
- 语气自然、可信、像专业顾问，不要夸张网红式表演
- 长口播必须拆成短镜头，方便后期调整顺序
- 使用 B-roll 覆盖口播剪切点，降低 Lip Sync 依赖

对白准确性重要时，提供两种方案：

1. **低成本无声画面版：** 只生成动作和表情，后期配音。
2. **原生对白版：** 写明准确台词、语言、说话人、情绪、语速和停顿。

## 游戏界面与 UI 规则

- 游戏界面只能作为 Hook、章节转场或简单选择画面。
- 模型只负责生成扫描线、轮廓框、光效、简单面板和留白区域。
- 准确文字、按钮、价格、电话、Logo 和复杂数据全部后期添加。
- 一镜只保留一个 UI 动作，例如扫描一次或进度环完成一次。
- UI 不得遮住人物脸、产品或车灯效果。
- 不要让整支广告都变成赛博游戏 CG。
- 证明效果的镜头要保持朴素真实，避免重特效让证据看起来造假。

## 每镜输出字段

每个正式镜头必须包含：

1. 镜头编号
2. 广告作用
3. 推荐生成模式
4. 时长与画幅
5. 所需参考图及用途
6. 起始构图
7. 主体与环境
8. 唯一主要动作
9. 表情和表演
10. 摄影机运动
11. 灯光和色彩
12. 一致性要求
13. 对白或旁白安排
14. 可直接使用的中文提示词
15. 负面限制
16. 后期添加内容
17. 风险等级
18. 低成本备用方案

提示词必须按时间顺序描述画面中实际发生的事情，使用明确动作，不使用“高级、史诗、爆款、电影感、超高清”等无法执行的空词，除非同时说明具体构图、光线、运动和材质。

## 文字、品牌与 CTA

优先后期添加：

- 中文字幕
- CTA
- 价格
- 电话号码
- WhatsApp 号码
- 菜单
- 进度条
- 复杂 HUD 标签
- 法律说明
- Before／After 标签
- Low Beam／High Beam 标签

模型画面可以保留简单形状、扫描线、光晕、空白面板或留白区。准确文字必须在“后期添加”中单独列出。

如果参考图里已有真实招牌或 Logo，只要求模型尽量保留，不得新增、改写或复制其他随机文字。

## 成本控制流程

生成顺序固定为：

1. 先生成一支 5 秒左右的人物一致性测试镜头。
2. 再生成一支 4–5 秒的产品或车辆准确度测试镜头。
3. 确认人物、服装、场景、车辆和整体风格。
4. 批量生成所有绿色低风险镜头。
5. 使用临时配音组一版粗剪。
6. 只生成粗剪中仍然缺少的黄色镜头。
7. 红色镜头必须拆分或替换后才允许尝试。
8. 最后添加字幕、UI、音效、Logo 和 CTA。

必须识别可以复用的素材：

- 同一 Talking Head 主场景可录多句口播
- 一支车辆展示镜头可裁成多个插入镜头
- 同一道路镜头可用于近光和远光版本
- 同一工作区可生成多个单动作施工镜头
- A／B Hook 只更换开头，主体广告尽量共用

## 抽卡失败处理阶梯

同一提示词失败后，禁止原样再跑：

1. 删除次要动作。
2. 固定摄影机。
3. 减少为一个主体和一个动作。
4. 把文字和 UI 移到后期。
5. 将一镜拆成两镜。
6. 先生成干净静帧，再图生视频。
7. 用更简单但能证明同一卖点的镜头替换。

## 默认输出顺序

### 1. 制作决策

说明推荐模式、音频方案、镜头数量、整体风险和最重要的省成本决定。

### 2. 全局一致性锁定

一次定义所有重复人物、服装、场景、车辆、产品和视觉风格。

### 3. 参考图准备清单

列出用户生成前必须准备的每张图或元素。

### 4. 镜头总表

列出时间、用途、模式、时长、参考图、风险和复用可能。

### 5. 逐镜中文提示词

每镜提供完整中文 Prompt、负面限制、后期叠加内容和备用方案。

### 6. 实际生成顺序

明确先测试哪两镜，再生成哪些低风险镜头，最后处理哪些高风险镜头。

### 7. 后期组装说明

说明配音、Lip Sync、字幕、UI、音效、转场和 CTA 应如何处理。

### 8. 执行交接

Prompt 包完成后不得自动开始生成。最后一句固定询问：

> 提示词已完成。是否继续运行 `my-higgs-vid`，按上述参考图顺序和参数生成视频？

如果当前页面已显示单镜 credits 或总预计消耗，必须把预计消耗写进这句询问。用户确认后，才把已批准的逐镜 Prompt、参考图顺序、模型参数、声音要求和成本上限交给 `my-higgs-vid`；确认只授权本轮已说明的生成，不包含自动重抽、购买 credits 或升级套餐。

## 完成前检查

交付前必须确认：

- 没有改变已批准的广告信息
- 第一帧有清楚的视觉概念
- 每镜只有一个主要动作
- 重复人物、服装、车辆、产品和场景均已锁定
- 每个高风险镜头都有稳定备用方案
- 准确文字已分配到后期，而不是强迫模型生成
- 提示词能在指定模型中执行
- 生成顺序不会浪费额度
- 每镜都明确写出要附哪些参考图及其用途
- 整份输出以中文为主，没有无必要的大段英文

最后指出整支视频最高风险的一个镜头及其推荐备用方案。两支锚点测试镜头未通过前，不得宣称整套视频已经可以直接批量生成。