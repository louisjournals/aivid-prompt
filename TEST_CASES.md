# my-aivid-prompt 测试场景

## 场景 1：低预算汽车 Talking Head 广告
输入：已经批准的 65 秒 Facebook 中文广告脚本、一套人物参考图、一张工作区参考图、一张车辆参考图、可灵 3.0、低抽卡预算。
预期：拆成短镜头；手势简单；文字与 UI 移到后期；每镜明确参考图用途；先测试两支锚点镜头；复杂游戏界面和道路对比必须给低成本备用方案；全部提示词使用中文。

## 场景 2：过度复杂的单一镜头
输入：主讲人边走进工作区边说话，同时指向移动中的车辆，悬浮 HUD 扫描车灯，背景还有技师维修。
预期：判定为红色高风险；拆成主讲人口播、静止车辆扫描和单动作施工三个镜头，不得原样写成一个 Prompt。

## 场景 3：准确广告文案和电话号码
输入：用户要求价格、WhatsApp 号码、Logo 和多行 CTA 直接出现在 AI 生成画面。
预期：只有可信参考图中已存在的文字可以尝试保留；其余准确文字全部分配到后期，不让模型凭空生成。

## 场景 4：更换模型
输入：已为可灵设计的分镜，用户临时改用其他模型。
预期：保留广告策略和镜头作用，只改写模型执行方式、参考图配置、镜头限制和备用方案。

## 场景 5：用户要求中文
输入：用户以中文要求生成 AI 视频提示词。
预期：所有镜头说明、Prompt、负面限制、生成顺序和后期说明均使用中文；只保留必要的品牌名、模型名和准确英文 UI 文案。

## 场景 6：多张参考图需要平台识别
输入：一张人物图和一张展示厅图，上传顺序为人物、展示厅。
预期：最终 Prompt 明确写 `@[Image 1](image_1)` 为唯一主讲人、`@[Image 2](image_2)` 为展示厅；不得只写“参考人物图”和“参考场景图”。

## 场景 7：Prompt 完成后的执行交接
输入：五个镜头 Prompt 已批准，网页显示每镜 8 credits。
预期：最后一句询问是否继续运行 `my-higgs-vid`，同时写明预计总消耗 40 credits；未得到确认前不执行生成，确认也不包含自动重抽或购买额度。

## 场景 8：新项目的视频参数不同
输入：上一项目使用 Kling 3.0 Omni、5 秒、9:16、720p、Sound On；新项目只给了脚本和参考图。
预期：不得继承旧参数。先用一张紧凑表一次性询问模型、每镜时长、画幅、分辨率、Sound、Multi-shot、每镜数量、重抽授权和 credits 上限；已知信息预填，确认后本轮不再重复询问。

## 场景 9：人物画面太像 AI 企业宣传片
输入：汽车灯光顾问站在 showroom，模型生成的人物完全居中、只拍到腰部、站姿僵直，头顶大面积白色灯板很亮，皮肤过度磨皮。
预期：Prompt 应默认改为膝盖以上、人物轻微偏左或偏右 10–15%、保留字幕空间、减少头顶留白与亮天花板、允许自然重心和轻微身体微动、保留真实皮肤纹理；明确禁止证件照感、新闻主播感、企业宣传片感和 AI 数字人感。除非 brief 明确要求，不把人物镜头写成过度 cinematic 或棚拍大片。

## 场景 10：my-vidwatch 高表现投放素材作为唯一结构输入
输入：`my-vidwatch` 的 `report.md` + 14 张 contact sheets；用户说明这三支是 20+ 条投放中 leads 最高的，没有提供分镜。
预期：不要求用户补分镜；从「哪里好」提取可复用素材；把「哪里要改」和跨视频缺陷当作假设而非事实；实际投放表现优先于 report 判断；涉及技术证明、实测数据、真实施工或真实场地的镜头判为必须实拍，而不是仅拆镜后交给 AI；先从 report + contact sheets 推导镜头清单并等待用户确认，确认后才写逐镜 Prompt。
